---
export interface Props {
  asset: string;
}

const { asset } = Astro.props as Props;
const { default: innerHTML } = await import(/* @vite-ignore */`/src/assets/svgs/${asset}.svg?raw`);
---

<Fragment set:html={innerHTML} />